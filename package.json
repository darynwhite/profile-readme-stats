{
    "private": "true",
    "scripts": {
        "build": "ncc build src/main.ts",
        "format": "prettier **/*.{ts,json,yml,md} --check",
        "format:fix": "prettier **/*.{ts,json,yml,md} --write",
        "ci": "npm run format:fix && npm run build"
    },
    "dependencies": {
        "@actions/core": "^1.11.1",
        "@octokit/graphql": "^7.1.0"
    },
    "devDependencies": {
        "@types/node": "^20.17.10",
        "@vercel/ncc": "^0.38.3",
        "prettier": "^3.4.2",
        "typescript": "^5.7.3"
    }
}
